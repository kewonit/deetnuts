# Legacy snapshots

Inactive source snapshots kept for reference. These files are not part of the
application and should not be imported directly.

The `cleanup-2026-08-19/` snapshot preserves the original repository paths for
files removed during the codebase cleanup on 2026-08-19. If anything is revived,
copy it back deliberately and update it to the current architecture first.
